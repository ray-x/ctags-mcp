# ctags-mcp

A lightweight MCP server that brings `ctags`-powered symbol search to AI coding clients and editors.

Use it to scan a workspace for symbols, inspect matching source snippets, and generate a BSD `ctags` tags file on demand.

## What it does

The server exposes two tools:

- `search_symbols`
- `generate_tags`

Arguments:

- `workspace_path` — absolute path to the project root to search
- `query` — optional filter for symbol name, file path, or source snippet

`generate_tags` writes a BSD `ctags` tags file for the workspace. It defaults to `workspace_path/tags`, and you can override the output path with `output_path`.

The default `tags` file is generated by the server and kept up to date automatically. It refreshes when the git worktree changes, and uses file mtimes for dirty working-tree files so repeated unstaged edits still trigger regeneration. Do not edit it by hand.

## Downloads

Latest binaries:

| Platform | Binary |
| --- | --- |
| macOS ARM64 | [ctags-mcp-darwin-arm64](https://github.com/ray-x/ctags-mcp/releases/latest/download/ctags-mcp-darwin-arm64) |
| Linux AMD64 | [ctags-mcp-linux-amd64](https://github.com/ray-x/ctags-mcp/releases/latest/download/ctags-mcp-linux-amd64) |
| Linux ARM64 | [ctags-mcp-linux-arm64](https://github.com/ray-x/ctags-mcp/releases/latest/download/ctags-mcp-linux-arm64) |
| Windows AMD64 | [ctags-mcp-windows-amd64.exe](https://github.com/ray-x/ctags-mcp/releases/latest/download/ctags-mcp-windows-amd64.exe) |

## Why choose this over other solutions?

- 🚀 **Zero Dependencies & Instant Setup:** Written in Go and compiled into a single executable binary. No Python environment, node_modules, or shell wrappers required. Just download and run.
- ⚡ **Outpaces Grep & LSPs:** Built specifically for massive, polyglot codebases. While LSPs struggle to initialize across multiple languages and `grep|rg` wastes time brute-forcing text matches, `ctags` indexes structure instantly.
- 🤖 **LLM-Optimized Output:** Designed from the ground up for Model Context Protocol (MCP) clients. It formats code definitions and context snippets natively for LLMs, saving token overhead while maximizing accuracy.

## Requirements

- Go 1.26+
- `ctags` available on `PATH`

## Build

```bash
go build -o ctags-mcp .
```

## Run

The server speaks MCP over stdio, so it is usually launched by a client.

## Claude Desktop config

Copy `claude_desktop_config.sample.json` to Claude Desktop's config location and replace the binary path with your local build.

## Copilot config

Copy `copilot-mcp-config.sample.json` to `~/.copilot/mcp-config.json` or the config location used by your Copilot client, then replace the binary path.

## Agent instructions

Claude and Copilot can use this MCP as soon as it is added to their MCP config.

Add a repo instruction file only if you want to guide how they use it.

This is most useful for active codebases where agents frequently search symbols, make refactors, or update code and you want the tags refresh behavior to stay automatic.

To help Claude or Copilot use this MCP automatically, add a repo instruction file such as `.github/copilot-instructions.md`:

```md
- Use the `ctags-mcp` MCP server for symbol lookup, definition navigation, and tags refresh in this repo.
- Prefer `search_symbols` over grep/rg when finding definitions, symbols, or cross-file references.
- Treat `tags` as generated output; do not edit it by hand.
- If code changes affect symbols, allow the server to refresh tags automatically or run `generate_tags` when needed.
```

## Example usage

```json
{
  "workspace_path": "/Users/you/src/my-project",
  "query": "handler"
}
```
